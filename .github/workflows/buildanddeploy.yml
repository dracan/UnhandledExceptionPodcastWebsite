name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    # actions-gh-pages only needs to push to gh-pages. Without this the job's
    # GITHUB_TOKEN gets the repository default, which may still be read/write
    # over issues, PRs and packages - handed to every step, including the
    # `pnpm install` that runs third-party lifecycle scripts.
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      # Fails the build on a disappeared URL, a broken image reference, an
      # unresolved icon name, a page missing from the sitemap, or a working
      # file about to be published. Runs before deploy so none of those ship.
      - name: Verify build
        run: pnpm run verify

      - name: Create CNAME file
        run: echo 'unhandledexceptionpodcast.com' > _site/CNAME

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
